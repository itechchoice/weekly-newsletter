---
slug: weekly-003
title: "Die Yi 技术周刊 #003"
authors: [editor]
tags: [ai, best-practices, github]
---

欢迎阅读 **Die Yi 技术周刊** 第 3 期！🎉

本期我们聚焦两个高频话题：AI 编码到底是提效还是返工加速器？以及一份可直接落地到项目中的 **React 18 Patterns** 规范清单。

让我们开始吧！

**本期目录：**

- 🔥 社区争议：AI 编码到底提效还是“返工加速器”？
- 💻 React 18 Patterns
- 📮 下期预告

<!-- truncate -->

---

## 🔥 社区争议：AI 编码到底提效还是“返工加速器”？

> **同样的 AI 工具，为什么有人提速，有人返工？**

### 痛点

团队里经常同时出现两种声音：

- “AI 帮我把开发速度拉起来了”
- “AI 代码上线前返工越来越多了”

如果没有统一的评估口径，最后就会变成“各说各话”，很难形成团队实践。

### 解决方案

把争议拆成“任务类型”来看，不做绝对判断。

| 证据类型 | 结论 | 你该怎么用 |
| ---- | ---- | ---- |
| 受控实验（经验开发者 + 成熟代码库） | 在复杂真实任务中，AI 可能拖慢（研究报告出现 +19% 用时） | 核心模块用 AI 时必须加严评审与回归 |
| 从业者调研 | 在小颗粒任务上普遍提效，但复杂度上升后收益下降 | 用 AI 处理样板代码、简单重构、文档更稳 |
| 社区讨论 | 高频争议点集中在“可维护性”和“隐性 bug” | 把“是否可维护”写进验收标准 |
| 工程实践 | AI 在“分解+草拟+批量修正”阶段收益明显 | 先拆任务，再让 AI 写，最后人工收敛 |

### 团队落地建议（简版）

1. 任务分层：把任务分成“低风险重复型”和“高风险核心型”。
2. 验收前置：先写验收点，再让 AI 开始生成。
3. 评审加固：核心模块要求人工逐段 review + 测试覆盖。
4. 结果复盘：按任务类型记录“节省时间/返工次数”。

🔗 **相关链接：**

- [Measuring the Impact of Early-2025 AI on Experienced Open-Source Developer Productivity](https://arxiv.org/abs/2507.09089)
- [The Impact of AI-Generated Solutions on Software Architecture and Productivity](https://arxiv.org/abs/2506.17833)

---

## 💻 React 18 Patterns

> **只讲 React 18 可落地规范，每条都给反例和正例。**

### 数据与并发

#### 1) 独立请求必须并行（`Promise.all`）

为什么：串行 `await` 会人为制造网络瀑布，放大等待时间。

**反例：**

```tsx
const user = await fetchUser();
const posts = await fetchPosts();
const comments = await fetchComments();
```

**正例：**

```tsx
const [user, posts, comments] = await Promise.all([
  fetchUser(),
  fetchPosts(),
  fetchComments(),
]);
```

适用边界：仅适用于请求间无依赖关系的场景。

#### 2) 用组件拆分消除数据瀑布

为什么：把多个 `await` 压在父组件里，会让子树被父级阻塞。

**反例：**

```tsx
async function Page() {
  const profile = await fetchProfile();
  return (
    <main>
      <Header profile={profile} />
      <Sidebar />
    </main>
  );
}

async function Sidebar() {
  const items = await fetchSidebarItems();
  return <nav>{items.map((x) => <div key={x.id}>{x.name}</div>)}</nav>;
}
```

**正例：**

```tsx
async function Header() {
  const profile = await fetchProfile();
  return <header>{profile.name}</header>;
}

async function Sidebar() {
  const items = await fetchSidebarItems();
  return <nav>{items.map((x) => <div key={x.id}>{x.name}</div>)}</nav>;
}

function Page() {
  return (
    <main>
      <Header />
      <Sidebar />
    </main>
  );
}
```

适用边界：服务端数据读取场景下优先拆分成可并行的子组件。

#### 3) 用分层 `Suspense`，不要整页阻塞

为什么：一个慢组件不应该卡住整页首屏。

**反例：**

```tsx
import { Suspense } from "react";

function Page() {
  return (
    <Suspense fallback={<div>Loading page...</div>}>
      <Hero />
      <SlowChart />
      <SlowFeed />
    </Suspense>
  );
}
```

**正例：**

```tsx
import { Suspense } from "react";

function Page() {
  return (
    <>
      <Hero />
      <Suspense fallback={<div>Loading chart...</div>}>
        <SlowChart />
      </Suspense>
      <Suspense fallback={<div>Loading feed...</div>}>
        <SlowFeed />
      </Suspense>
    </>
  );
}
```

适用边界：对独立可加载区块单独设边界，别把全页包成一个边界。

#### 4) 避免 barrel imports，直接按文件导入

为什么：barrel 入口经常把大量无关模块拉进构建和运行时路径。

**反例：**

```tsx
import { Search, Bell, User } from "lucide-react";
```

**正例：**

```tsx
import Search from "lucide-react/dist/esm/icons/search";
import Bell from "lucide-react/dist/esm/icons/bell";
import User from "lucide-react/dist/esm/icons/user";
```

适用边界：大型组件库、图标库优先 direct import。

#### 5) 重组件按需动态加载

为什么：把低频重组件塞进首包会拖慢首屏与交互时间。

**反例：**

```tsx
import BigEditor from "./BigEditor";

export default function PostPage() {
  return <BigEditor />;
}
```

**正例：**

```tsx
import React, { Suspense, lazy } from "react";

const BigEditor = lazy(() => import("./BigEditor"));

export default function PostPage() {
  return (
    <Suspense fallback={<div>Loading editor...</div>}>
      <BigEditor />
    </Suspense>
  );
}
```

适用边界：编辑器、图表、富文本等重模块优先懒加载。

### 渲染与 Hydration

#### 6) 不要在 SSR 首次渲染直接读浏览器 API

为什么：`window`、`localStorage` 在服务端不存在，会直接报错。

**反例：**

```tsx
function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme] = React.useState(localStorage.getItem("theme") || "light");
  return <div className={theme}>{children}</div>;
}
```

**正例：**

```tsx
function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = React.useState("light");

  React.useEffect(() => {
    const saved = localStorage.getItem("theme");
    if (saved) setTheme(saved);
  }, []);

  return <div className={theme}>{children}</div>;
}
```

适用边界：所有浏览器 API 访问都应放在客户端时机（如 `useEffect`）。

#### 7) Hydration 防闪烁要前置同步修正

为什么：先渲染默认值再 `useEffect` 修正，会产生可见闪烁。

**反例：**

```tsx
function AppShell() {
  const [theme, setTheme] = React.useState("light");
  React.useEffect(() => {
    setTheme(localStorage.getItem("theme") || "light");
  }, []);
  return <div className={theme}>...</div>;
}
```

**正例：**

```tsx
function AppShell() {
  return (
    <>
      <script
        dangerouslySetInnerHTML={{
          __html: `
            (function () {
              try {
                var t = localStorage.getItem('theme') || 'light';
                document.documentElement.dataset.theme = t;
              } catch (e) {}
            })();
          `,
        }}
      />
      <div>...</div>
    </>
  );
}
```

适用边界：主题、语言、布局偏好这类“首屏即敏感状态”优先前置修正。

#### 8) 多实例 DOM 定位用 `useId`

为什么：硬编码 id 在多实例场景会冲突，导致错误挂载。

**反例：**

```tsx
function ThemeBlock() {
  return <div id="theme-root" />;
}
```

**正例：**

```tsx
function ThemeBlock() {
  const id = React.useId();
  return <div id={id} />;
}
```

适用边界：组件可能被复用多次时，不要写死 DOM id。

### 状态与副作用

#### 9) 用户交互副作用写在事件里，不写“state + effect”模拟

为什么：把交互副作用挂到 effect，容易被依赖变动重复触发。

**反例：**

```tsx
function Form() {
  const [submitted, setSubmitted] = React.useState(false);

  React.useEffect(() => {
    if (submitted) {
      post("/api/register");
      toast("Done");
    }
  }, [submitted]);

  return <button onClick={() => setSubmitted(true)}>Submit</button>;
}
```

**正例：**

```tsx
function Form() {
  function handleSubmit() {
    post("/api/register");
    toast("Done");
  }

  return <button onClick={handleSubmit}>Submit</button>;
}
```

适用边界：凡是“点击就执行”的副作用，优先事件处理函数。

#### 10) 派生状态在 render 期计算，不建镜像状态

为什么：镜像状态会产生额外同步成本和不一致风险。

**反例：**

```tsx
function Cart({ items }: { items: { price: number }[] }) {
  const [total, setTotal] = React.useState(0);

  React.useEffect(() => {
    setTotal(items.reduce((s, x) => s + x.price, 0));
  }, [items]);

  return <p>Total: {total}</p>;
}
```

**正例：**

```tsx
function Cart({ items }: { items: { price: number }[] }) {
  const total = items.reduce((s, x) => s + x.price, 0);
  return <p>Total: {total}</p>;
}
```

适用边界：只要值能从现有 props/state 直接推导，就不要单独存 state。

#### 11) `setState` 用函数式更新

为什么：避免闭包拿到旧值，保证并发更新下逻辑稳定。

**反例：**

```tsx
function Counter() {
  const [count, setCount] = React.useState(0);

  const inc = () => {
    setCount(count + 1);
    setCount(count + 1);
  };

  return <button onClick={inc}>{count}</button>;
}
```

**正例：**

```tsx
function Counter() {
  const [count, setCount] = React.useState(0);

  const inc = () => {
    setCount((c) => c + 1);
    setCount((c) => c + 1);
  };

  return <button onClick={inc}>{count}</button>;
}
```

适用边界：新状态依赖旧状态时，一律用函数式更新。

#### 12) 昂贵初始值用 `useState` 懒初始化

为什么：避免每次 render 都重复执行昂贵计算。

**反例：**

```tsx
function Report() {
  const [data] = React.useState(buildBigDefaultData());
  return <pre>{JSON.stringify(data)}</pre>;
}
```

**正例：**

```tsx
function Report() {
  const [data] = React.useState(() => buildBigDefaultData());
  return <pre>{JSON.stringify(data)}</pre>;
}
```

适用边界：初始化成本明显时使用懒初始化；轻量值可直接赋值。

#### 13) 非紧急更新用 `startTransition`

为什么：把低优先级更新标记为 transition，可避免输入/滚动卡顿。

**反例：**

```tsx
function SearchBox() {
  const [query, setQuery] = React.useState("");
  const [list, setList] = React.useState<string[]>([]);

  function onChange(e: React.ChangeEvent<HTMLInputElement>) {
    const q = e.target.value;
    setQuery(q);
    setList(expensiveFilter(q));
  }

  return <input value={query} onChange={onChange} />;
}
```

**正例：**

```tsx
import { startTransition } from "react";

function SearchBox() {
  const [query, setQuery] = React.useState("");
  const [list, setList] = React.useState<string[]>([]);

  function onChange(e: React.ChangeEvent<HTMLInputElement>) {
    const q = e.target.value;
    setQuery(q);
    startTransition(() => {
      setList(expensiveFilter(q));
    });
  }

  return <input value={query} onChange={onChange} />;
}
```

适用边界：适合“可延后”的计算结果，不用于必须立即反馈的核心输入值。

#### 14) 高频瞬时值放 `useRef`

为什么：高频更新若放 state，会触发大量无意义重渲染。

**反例：**

```tsx
function PointerTracker() {
  const [pos, setPos] = React.useState({ x: 0, y: 0 });

  React.useEffect(() => {
    const onMove = (e: MouseEvent) => setPos({ x: e.clientX, y: e.clientY });
    window.addEventListener("mousemove", onMove);
    return () => window.removeEventListener("mousemove", onMove);
  }, []);

  return <div>{pos.x}, {pos.y}</div>;
}
```

**正例：**

```tsx
function PointerTracker() {
  const posRef = React.useRef({ x: 0, y: 0 });

  React.useEffect(() => {
    const onMove = (e: MouseEvent) => {
      posRef.current = { x: e.clientX, y: e.clientY };
    };
    window.addEventListener("mousemove", onMove);
    return () => window.removeEventListener("mousemove", onMove);
  }, []);

  return <button onClick={() => console.log(posRef.current)}>Log pointer</button>;
}
```

适用边界：仅用于“变化频繁但不需要立即驱动 UI 重绘”的值。

### 组件健壮性

#### 15) Portal/iframe 场景绑定正确 window（`ownerDocument.defaultView`）

为什么：直接绑全局 `window`，在弹窗窗口/iframe 中会失效。

**反例：**

```tsx
function Hotkey() {
  React.useEffect(() => {
    const handler = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key === "k") console.log("open");
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, []);

  return <div>Hotkey enabled</div>;
}
```

**正例：**

```tsx
function Hotkey() {
  const ref = React.useRef<HTMLDivElement | null>(null);

  React.useEffect(() => {
    const win = ref.current?.ownerDocument.defaultView || window;
    const handler = (e: KeyboardEvent) => {
      if ((e.metaKey || e.ctrlKey) && e.key === "k") console.log("open");
    };
    win.addEventListener("keydown", handler);
    return () => win.removeEventListener("keydown", handler);
  }, []);

  return <div ref={ref}>Hotkey enabled</div>;
}
```

适用边界：组件可能运行在 portal、iframe、popout window 时必须用此模式。

### 本章来源

- [React Stack Patterns (2026)](https://www.patterns.dev/react/react-2026)
- [Building Bulletproof React Components](https://shud.in/thoughts/build-bulletproof-react-components)
- [Vercel React Best Practices](https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices)

---

## 📮 下期预告

下周我们将关注：

- AI 代码评审清单：哪些问题最容易漏
- 前端性能体检：从 bundle 到交互延迟

---

_有推荐的项目或话题？欢迎反馈给我们！_
